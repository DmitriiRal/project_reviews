package Steam

case class GameSearch(
                       name: String,
                       id: Long,
                       steamId: Long,
                       gamePicture: String
                     )
